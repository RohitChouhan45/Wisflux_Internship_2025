# JWT Auth Demo

A simple Node.js + Express project demonstrating JWT-based user authentication.

## Features
- Login route returns a JWT token
- Protected route `/dashboard` accessible only with a valid token

## Setup
```bash
npm install
npm start
